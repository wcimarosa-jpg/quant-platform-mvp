# MCP Coordinator Placement

Place the MCP sprint coordinator implementation here.

Current implementation exists at:

- `../../mcp_sprint_coordinator` (workspace root sibling)

Recommended action:

1. Copy that folder contents into this `mcp/coordinator/` path, or
2. Keep existing location and point team docs/scripts to it consistently.
